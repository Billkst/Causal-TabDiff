# Agent 核心执行准则

在执行任何任务或编写新代码之前，你**必须强制执行**以下步骤：

1. **主动嗅探技能 (Skill Check)：** 接收任务后，第一步必须主动扫描当前目录（及子目录），查找现存的工具脚本或 Skills。
2. **拒绝重复造轮子：** 评估找到的现有 skill。如果适用，**必须优先调用**，严禁编写功能重复的代码。
3. **按需新建：** 只有在完全确认本地没有合适的 skill 时，才允许开始编写新代码。