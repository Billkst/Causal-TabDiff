# Causal-tabdiff 项目规则
 
## 强制执行前置检查

在开始规划或执行任何任务之前，**必须优先检查 `skills` 目录**中是否存在可用skill。绝对禁止跳过此步骤直接执行任务或编写新代码。

## Conda 环境

本项目的conda环境为causal_tabdiff，启动命令为conda activate causal_tabdiff

## 语言设置

始终以简体中文回复